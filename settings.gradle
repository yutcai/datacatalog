plugins {
    // Auto-provisions the JDK declared by the toolchain (Java 21) if not installed locally
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'datacatalog'
